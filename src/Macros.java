package src;

public class Macros {
    // Because they're final, each of these can be used like a C macro.

    // Print messages about the terms as they're loaded.
    public static final boolean DEBUG_TERMS = false;

    // Print messages about the decompilation and scanning process.
    public static final boolean DEBUG_DECOMPILATION = false;

    // Print messages about the tasks, pool, and file-scanning.
    public static final boolean DEBUG_TASKPOOL = false;
}
